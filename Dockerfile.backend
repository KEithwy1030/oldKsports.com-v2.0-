FROM node:22-alpine

LABEL "language"="nodejs"
LABEL "framework"="express"

WORKDIR /app

# 安装后端依赖
COPY server/package*.json ./
RUN npm ci --only=production

# 复制后端代码
COPY server/ ./

EXPOSE 8080

CMD ["npm", "start"]

